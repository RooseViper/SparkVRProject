using System.Collections;
using System.Collections.Generic;
using Nova;
using UnityEngine;

public class ShowcaseTable : MonoBehaviour
{
    [SerializeField] private TextBlock yearTextBlock, titleTextBlock, messageTextBlock;
    [SerializeField] private UIBlock2D displayUIBlock;
    // Start is called before the first frame update
    private void Start()
    {
        
    }
}
